# Текущий шаг

Разделы 1–5 закрыты, открытыми остались 1.6 за владельцем и
отложенный 4.3. Вопросы 3.1 и 3.2 закрыты ответами без правок в коде.

Три бага с живого окна вылечены и проверены владельцем: перевод
описания, виджеты после молчания базы, очередь перевода. Пункты —
в разделе 5 плана, причины решений — в `DECISIONS.md` этой же папки.

Последняя проверка чистая: тайпчек молчит, обе сборки и релизная
сборка оболочки проходят, окно запускается.

Следующий шаг: владелец ищет, что ещё войдёт в патч. Выпуск берём
только по его команде: номер, `CHANGELOG.md`, слияние и тег.

Что учесть:

- Номер версии поднимается в двух местах: `package.json` и `src-tauri/Cargo.toml`.
- Новая команда оболочки — четыре места: `lib.rs`, `build.rs`, `capabilities/default.json` и контракт `IBridge.ts` с обеими реализациями.
- Общее состояние блока навигации живёт в `nav-state.ts`: импорт из `NavPanel.vue` в `nav.ts` замкнул бы граф.
- Запись файла через API рвётся около 34 КБ: длинный исходник либо режем, либо перечитываем хвост сразу после записи.
- `src/style.scss` — 46 КБ, через API целиком не пройдёт: резать на части либо отдать владельцу.
- Резка style.scss по номерам строк — только с проверкой баланса скобок после: перевёрнутый диапазон в PowerShell возвращает строки в обратном порядке, а не пустоту.
- cargo check требует сначала `npm run build:tauri`: `lib.rs` вклеивает `dist/animori.tauri.*`.
- Формат проверять до коммита: Prettier сам сжимает импорты и атрибуты до ста символов, ручной перенос он считает ошибкой.
- `SettingsProxyCard.vue` не проходит Prettier и без наших правок — старый долг, чинить отдельным коммитом.
- Наличие ключа в собранном бандле ничего не доказывает: алиасы `@*-impl` подменяют реализацию, смотреть надо в код, а не grep по строке.
- Жалоба обновлятора на `latest.json` в отладочном запуске — шум: файл появляется только в выпущенном релизе.
- Смена дефолтов дойдёт до людей только выпуском — раздел 6 плана.
- Сохранённый выбор тех, кто тумблер включал, остался как был.
