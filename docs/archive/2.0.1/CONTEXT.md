# Контекст текущего шага

Шаг: уборка самой документации по `docs/DOC-RULES.md`. План сведён к
строкам с галочкой, долги разобраны по местам.

Предыдущий шаг — обратная связь — закрыт, решения в `DECISIONS.md`
этой же папки, хеши — в `PLAN.md`. Владелец проверил: `typecheck`
чист, обе сборки зелёные, окно запускается.

Живое ограничение шага: GitHub читает `.github/ISSUE_TEMPLATE` только из
ветки по умолчанию. До мерджа в `main` вкладка Issues показывает пустой
шаблон, а ссылки из приложения теряют подстановку. Ждём релиза,
отдельного коммита в `main` не делаем.

Дальше по плану: шапка `ActionPanel.vue` под лимиты стиля, затем три
карты под этапы 6 и 7. Пункт 5.4 заморожен.
