# Порядок работ

Что делается и в какой очерёдности. Порядок выстроен по зависимостям:
каждый следующий этап опирается на предыдущий, а не просто идёт за ним.

Почему именно эти задачи — в `DECISIONS.md`. Здесь только порядок.

Порядок относится к приложению. Скрипт с августа 2026 года живёт
в ветке `script` и своим планом; сюда его задачи не попадают.

---

## Этап 1. Расчистка

Ничего нового не добавляется. Цель — уменьшить количество кода,
который придётся трогать на этапе 2.

### 1.1. Снять мангу — сделано

Прошло сверху вниз, как и было задумано: экраны, ядро, запросы, обоснование
запасного источника в `settings.ts`. На каждом шаге проект оставался
собираемым, поэтому правки разложены по отдельным коммитам, а не одним валом.

Что сделано по существу:

- Экраны и подписи говорят только об аниме: `labels.ts` без ветвления по виду
  тайтла, части — всегда серии, поле томов из окна правки убрано.
- Ядро: снимок поднят до шестой версии, вид тайтла и тома из записей ушли,
  вида правки `volumes` больше не существует.
- Запросы: во всех обращениях к AniList вид тайтла — слово `ANIME` в теле
  запроса, русский поиск ходит только в раздел аниме Шикимори, anime365
  аргумент вида тайтла игнорирует.
- Франшиза и рекомендации отбрасывают мангу в разборщике ответа. У франшизы
  это заодно обесценивает старые записи кэша — цена в один лишний запрос
  на тайтл при первом открытии.

Проверка снимка выполнена так, как записано: снимок со старыми записями манги
открывается, лишние записи отбрасываются при подъёме. Правка томов, застрявшая
в очереди с прошлых версий, обработчика не найдёт — она выпадет с записью
в журнал.

Решено и **не** снято — перечень остатков лежит в разделе «Остатки манги»
в `ARCHITECTURE.md`. Прежнее обоснование «это нужно юзерскрипту, который
работает на манговых страницах сайта» после разделения продуктов
недействительно: здесь эти объявления — мёртвый груз, и снимаются они
пунктом 1.4.

### 1.2. Разрезать нарушителей потолка — закрыто вместе с потолком

Два экрана разрезаны по ходу 1.1, потому что иначе их было не отредактировать:

| Файл              | Было   | Стало                                        |
| ----------------- | ------ | -------------------------------------------- |
| `MediaScreen.vue` | 45 762 | 11 608 + `media-card.ts` + `media-screen.css` |
| `ListsScreen.vue` | 25 646 | 18 440 + `lists-row.ts`                      |

Резали по роли, а не по размеру: разметка остаётся в `.vue`, сбор данных
уезжает в `.ts`, стили — в отдельный файл. Раскладка описана
в `ARCHITECTURE-UI.md`.

По той же границе разрезан `snapshot.ts`: снимок остался в `snapshot.ts`,
очередь правок уехала в `edit-queue.ts`, общее звено записей —
в `store-chain.ts`. Границу задал предмет, а не размер.

Дальше резать нечего, и пункт закрыт не работой, а снятием повода. Потолок
в 25 КБ держался на молчаливом обрыве правок через API; правки идут прямо
в рабочем дереве, обрыва нет, и `auth.rs` остаётся крупнейшим исходником
законно — предмет в нём один. Что осталось от правила — резать
по предметной области и перечитывать записанное, `CONVENTIONS.md`.

### 1.3. Мелкие долги

Закрыто:

- `getDbStats` считает все префиксы склада, остаток уходит в `other`.
- При отказе сервера AniList в журнал уходит выписка заголовков отказа
  (`cf-ray`, `cf-mitigated`, `cf-cache-status`, `retry-after`,
  `x-ratelimit-*`, `server`). 403 по-прежнему неоднозначен, но теперь
  по журналу видно, кто ответил — сервис или его защита.
- Склад переименован. Физический стор — `mediaCache`, база шестой версии,
  перенос записей курсором внутри миграции, старый стор удаляется только
  после удачного переноса. Старое имя `shikiCache` осталось устаревшим
  псевдонимом и разрешается внутри `db.ts`.
- Мёртвые экспорты сняты: `LOG_LIMIT` в журнале был псевдонимом `LOG_CAPACITY`
  без единого потребителя, а `getStoredAlToken()` после снятия чтения Vuex
  повторял `getAlToken()`. Ссылка на несуществующий `logger-state.ts` убрана.
- Мёртвые классы `.am-cl-*` и `.am-extlink-tag` из плана ушли вместе
  с `style.scss`: чистить их надо в ветке `script`, а не здесь.

- Читатель журнала есть: экран `app/screens/LogScreen.vue`, вход кнопкой
  из «О программе». Отбор по виду записи, разворачивание подробностей,
  копирование показанного в буфер. `registerLogSink` наконец зовут,
  рядом появились `readLogs` и `clearLogs` — подписка отдаёт поток,
  а читателю нужна и предыстория.

  Долг закрыт со стороны читателя, а не тумблера, и заодно выяснилось,
  что тумблера не существует: `enableLogger` в `src/app` не встречается
  вовсе, ключ `set_logger` только читается при загрузке настроек.
  Прежняя запись здесь обещала выбор, которого не было.

Снято как неверное: прежняя запись обещала, что `collectEpisodes` хранит
адреса серий и потому вход в цепочку Kodik открыт. `collectEpisodes` живёт
в ветке `script`, а здесь его нет вовсе — вместе со всем клиентом Kodik.
Вход в цепочку действительно открыт с первого шага, но по другой причине:
поиск с полем `with_episodes_data` отдаёт адреса серий сразу. Проверено
пробой 4.0.

### 1.4. Хвосты расчистки — закрыто

Каждый пункт читался из нескольких мест и правился вместе с ними,
поэтому и не снимался «за компанию». Что сделано:

- Вызовы склада переведены на настоящее имя `mediaCache`; `MediaCacheRecord`
  стал основным типом. Псевдоним `shikiCache` остался и в имени стора,
  и в типе — ради ветки `script`, где лежит то же ядро со старыми вызовами.
  Имя склада меняется либо в двух ветках сразу, либо ни в одной.
- Аргумент `_type` снят из двадцати трёх функций и всех мест вызова —
  разом, потому что иначе `vue-tsc` валится на числе аргументов.
  Слово `'ANIME'` осталось там, где оно значение поля формы, а не аргумент.
- Мёртвые экспорты `media-title.ts` сняты все три: `peekRussianTitle`,
  `getRussianName`, `prefetchRussianTitles`. Список из плана подтвердился
  обходом вызовов целиком.
- Поля вида и томов ушли из `EntryFilter` вместе с `totalVolumes` —
  единственным читателем `SnapshotEntry.volumes`.
- Ключи `enableLinkMangalib` и `mangalibDomain` сняты: `media-links.ts`
  их не спрашивал.
- Связка `<style scoped src="./media-screen.css">` проверена по собранному
  CSS: все селекторы файла получают `data-v-`, плагин Vue обрабатывает её
  как ожидалось. Ни выносить блоки в компоненты, ни снимать `scoped`
  не нужно.

Сверх плана в ядре нашлось больше: мертвы были все двадцать два регэкспа
переводчика разметки, а не только три с главами и томами, и вместе с ними
словари месяцев и сезонов с палитрой ссылок. Это остатки надстройки над
чужой страницей — своему окну переводить разметку незачем.

`MediaType` намеренно оставлен: `franchise.ts` и `shikimori-people.ts`
им действительно отбрасывают мангу из чужих ответов, где она есть.

### 1.5. Разрезать документацию — сделано

`ARCHITECTURE.md` вырос до 51 КБ и перестал правиться целиком. Разрезан
на четыре части: обзор и ядро остались в `ARCHITECTURE.md`, экраны —
в `ARCHITECTURE-UI.md`, видео и плеер — в `ARCHITECTURE-VIDEO.md`, оболочка
и сборка — в `ARCHITECTURE-SHELL.md`. Надстройка над чужой страницей
и блокировщик из этих документов ушли вместе с продуктами, которым
они принадлежали.
Дробить мельче намеренно не стали: десяток мелких файлов труднее держать
в согласии, чем четыре тематических.

---

## Этап 2. Датасет названий

Полный разбор — в `DATA-PIPELINE.md`. Кратко:

1. Разделить путь имени и путь карточки — **сделано.** Разрез прошёл
   не в `api/titles.ts`, а слоем выше, в `core/media-title.ts`: имя живёт
   в своей записи склада (`NAME1_`), карточка с описанием, ссылкой
   и оценками — в своей (`RU3_`). Сетки, полки, выдача поиска и полка
   франшизы зовут `prefetchRussianNames`, поиск по коллекции греет склад
   имён, описание берёт только открытая карточка. Датасету есть куда лечь.
2. Проба обхода с руннера — **сделано.** Три тысячи номеров выяснили
   главное: адреса датацентра Шикимори не отклоняет — шестьдесят
   запросов подряд без единого отказа, и русское имя есть почти
   у девяти тайтлов из десяти.
3. Завести `animori-data`, собрать первый выпуск руками — **сделано.**
   Выпуск `2026.08.22-2005`: 30 215 записей, из них 26 011 с русским
   названием, 1.04 МБ имён и 106 КБ карты. Лицензия легла вместе
   с репозиторием, до первого выпуска.
4. Научить клиент читать датасет — **сделано.** Путь имени теперь
   пятиступенчатый: память запуска, датасет, склад имён, склад карточек,
   сеть. Файл `animori-dataset.json` лежит в приватном каталоге
   программы, скачивается по описи с проверкой отпечатков, обновляется
   в фоне после старта. Пустое поле в выпуске читается как ответ
   «перевода нет» и сеть не тревожит. Состояние датасета видно человеку
   в «О программе»; собственного просмотрщика журнала у приложения нет,
   и полагаться на него в проверках нельзя.
5. Расписание и личный токен вместо встроенного — **сделано.**
   Расписание встало: понедельник, 04:27 UTC, плюс запуск кнопкой.
   Секрет `ANIMORI_DATA_TOKEN` остался необязательным запасом, а не
   обязанностью: он лечит засыпание расписания после шестидесяти дней
   без человеческой активности, но и сам однажды протухает, тоже молча.
   Поэтому за живостью выпусков следит сторож `watch-dataset` — он смотрит
   на возраст выпуска и ловит любую причину протухания, а не только
   заснувшее расписание. Работает он из `main`, потому что расписание
   GitHub заводит прогоны только из ветки по умолчанию; копия лежит
   и здесь, байт-в-байт та же. Памятка — `DATASET-MAINTENANCE.md`.
6. Подпись описи — **сделано.** У имён в описи теперь три числа рядом
   с `count`: `russian` (записи с русским именем), `cyrillic` (из них
   с кириллицей) и `known` (доля узнанных Шикимори номеров). Смысл
   самого `count` не менялся намеренно — поле уже читают, и переопределить
   его значило бы соврать всем, кто на него оперся.
7. Две волны обогащения в сборщике — **сделано.** Волна карты добирает
   у AniList пары, которых нет у манами; волна имён добирает с anime365
   названия, которых не дал Шикимори, и принимает только кириллицу.
   Обе необязательные и идут после порогов приёмки. Разбор —
   в `DATA-PIPELINE.md`.

Что осталось открытым: числа волн на живом выпуске. Единственный
выпущенный датасет собран до их появления, следующая недельная сборка —
первая с ними. Пока разрыв между 18 858 парами и 30 215 именами
в документации описан как факт первого выпуска, а не как постоянное
свойство.

Признак успеха: список на сотню записей на чистой установке показывает
кириллицу сразу, без сети вообще. На живой установке это уже так —
списки и карточка выходят на русском без ожидания. Замер именно
на чистой установке, где файла датасета ещё нет, не делался.

Оба открытых вопроса решены до шага 4.

Первый — источник по умолчанию. Главным в настройках стоит anime365,
а датасет собран по Шикимори. Решение: в пути имени датасет стоит выше
сети — он свежее и полнее любого сетевого источника. Цена решения —
редкое расхождение: имя в сетке может отличаться от имени, приехавшего
с описанием в открытую карточку. Принято осознанно.

Второй — покрытие карты. В первом выпуске карта MAL — AniList покрывала
18 858 записей из 30 215, а клиент живёт на номерах AniList. Ответ нашёлся
замером по коду, без живой коллекции: `idMal` приходит вместе с медиа
в поиске, карточке и работах студии, а в список он добавлен в `LIST_QUERY`
тем же этапом и хранится в снимке. Карта остаётся страховкой для тайтлов
без `idMal`.

Сверх того разрыв закрывается со стороны сборщика: волна карты спрашивает
недостающие пары у самого AniList. Насколько закроется — покажет опись
первой сборки с волнами.

---

## Этап 3. Сохранность списков

Полный разбор — в `STORAGE-AND-SYNC.md`. Кратко:

1. Разделить доставленную правку и брошенную. Сейчас обе ведут
   в `markEditAccepted`, и хранилище их не различает.
2. Идентификатор устройства.
3. Формат журнала и сведение нескольких журналов — на обычном каталоге,
   без облака.
4. Выбор каталога в настройках и опознание пропажи.
5. Сворачивание своего журнала.

Первые три шага проверяются на одной машине двумя каталогами. Облако
подключается последним, когда сведение уже работает.

Долг размера снят до начала этапа: `snapshot.ts` разрезан по границе
«снимок против очереди правок», очередь живёт в `edit-queue.ts`, общее звено
записей — в `store-chain.ts`. Первый шаг делается теперь в `edit-queue.ts`
и `edit-sender.ts`.

---

## Этап 4. Свой плеер

Полный разбор — в `ARCHITECTURE-VIDEO.md`. От датасета и списков этот этап
не зависит вовсе — он стоит после них потому, что терять списки хуже,
чем смотреть в чужой кадр.

### 4.0. Проба цепочки руками — сделано

Пройдена 01.09.2026 на одном тайтле, ни строки кода в проекте. Цепочка
оказалась длиннее задуманной: поиск → три поля из адреса → страница серии
за подписями и именем связки → связка за адресом приёмника и шифром →
запрос → расшифровка. Полное описание с измерениями —
в `ARCHITECTURE-VIDEO.md`.

Три величины, которых требовал этот пункт:

- **Адрес приёмника** — `/ftor`, лежит в связке вызовом `atob`. Хардкодить
  нельзя, и не столько из-за адреса, сколько из-за имени самой связки:
  в нём `sha256`, и оно меняется с каждым их выпуском.
- **Сдвиг** — 18 на день пробы, но в код он не попадёт даже подсказкой.
  Перебор 1..25 с проверкой на `.m3u8` дал ровно одно подходящее значение,
  значит величина добывается, а не задаётся.
- **Срок жизни** — стоит в самой ссылке открытым текстом, с точностью
  до часа, и здесь была моя ошибка. Прежняя запись обещала, что ссылка,
  взятая за минуту до границы, умрёт на первой минуте эпизода. Петля
  замеров показала другое: метка — это час выдачи плюс два по Москве,
  а не сам час выдачи, и ссылка с меткой `18` прожила не менее двух часов
  сорока семи минут. Точная граница ещё измеряется: 20:00 либо 21:00 МСК,
  то есть зона метки — UTC+1 либо UTC. В коде взят ранний край: лишний
  пересчёт цепочки дешевле чёрного экрана. Обновление на ходу обязательно
  в любом случае, но не «каждый час», а по метке плюс запас.

Сверх плана выяснилось четыре вещи, и каждая меняет код: тип серии бывает
`seria`, поток раздаёт `solodcdn.com`, а не `kodik-cdn.com`, у каждого
качества свой манифест на своей площадке, и заголовки потоку не нужны
вовсе — CORS открыт настежь.

### 4.1. Плеер на Aniliberty — сделано

Слой источников лёг так, как задумывалось: `core/video.ts` держит типы,
реестр, проверку срока и выбор дорожки, `api/video-sources.ts` — единственное
место со списком резолверов, `api/aniliberty.ts` — первый из них. Ядро
не знает ни одного адреса, зависимости идут api → core.

Aniliberty выбран не как фоллбэк, а как единственный источник, где ссылки
лежат в открытом API и поведение предсказуемо. Порядок именно такой потому,
что обратный значит отладку своего плеера и чужого недокументированного
формата одновременно. Когда сломалось всё сразу, непонятно, что именно
сломалось.

### 4.2. Экран вместо оверлея — сделано

Плеер живёт отдельным экраном приложения: `player` в `SCREEN_NAMES`, подпись
в `SCREEN_TITLES`, разбор адреса `#/player/<id>` через `SCREENS_WITH_ID`,
компонент в `App.vue` ленивой загрузкой. Состояние — `screens/player-view.ts`,
воспроизведение — `screens/player-hls.ts`, разметка — `PlayerScreen.vue`,
стили — `player-screen.css`. В меню экран не попал намеренно: попадать туда
со страницы тайтла, кнопкой «Смотреть».

`hls.js` — первая внешняя библиотека в проекте. Грузится только вместе
с экраном просмотра, а на платформах с родным `m3u8` не трогается вовсе.
Вес сборки проверить на первом же `build:app`.

Прокси через оболочку здесь не нужен: проба 4.0 показала
`access-control-allow-origin: *` и на манифесте, и на сегменте, для любого
источника запроса. Библиотека тянет поток прямо из WebView. Автовыбора
битрейта не будет — манифест одиночный, вариантов внутри нет, поэтому
переключение качества делает сам экран подменой источника с восстановлением
позиции.

Место остановки живёт в памяти запуска, а не в базе: первые десять секунд
за просмотр не считаются. Перенести в снимок — отдельная задача, она
не из этого этапа.

### 4.3. Резолвер Kodik к готовому плееру — написан, ждёт живой проверки

Цепочка из шага 4.0 легла кодом в `api/kodik.ts` с тремя жёсткими правилами,
и все три выполнены: сдвиг добывается перебором 1..25 с памятью на запуск,
ссылки не кэшируются никогда, качества сворачиваются по адресу, а не
по ключу ответа — у пробы 480 и 720 вели на один файл. Написано заново
по описанию протокола: чужая реализация, по которой цепочка сверялась,
под лицензией семейства GPL, а у нас MIT.

Кэшируется только выборка озвучек, и то на десять минут: адреса страниц
серий постоянные, а вот подписи со страницы и ответ приёмника берутся
в последний момент, при каждом открытии серии.

Вход в цепочку открыт с первого шага: поиск с полем `with_episodes_data`
отдаёт адреса серий сразу, лишнего запроса резолверу не нужно.

Правка разрешений, без которой шаг не работал, сделана 01.09.2026: в http
открыты `kodik-api.com`, `kodikplayer.com`, `*.solodcdn.com`,
`*.kodik-cdn.com` и `i.kodikres.com`. Шаблоны по поддоменам, а не точные
имена, потому что номер площадки раздачи плавает даже между качествами
одной серии.

Чего ещё не было: цепочка не проходила из собранного приложения ни разу —
все измерения сняты руками в пробе. Первый живой прогон покажет две вещи,
которых проба знать не могла: пропускает ли мост заголовки страницы серии
и ведёт ли себя приёмник так же из WebView, как из консоли.

Признак успеха этапа: серия играет в своём плеере с пульта, без мыши,
из любого из двух источников, и переключение источника не видно
в интерфейсе.

---

## Этап 5. Люди в датасете — решено не делать

Имена и описания людей остаются в рантайме, датасет людей не собирается.
Правило отбора датасета — «в файл едет то, что нужно сотнями сразу», —
а люди нужны десятками и только на открытой карточке. Карточка сетевая
по своей природе: обложки и описание идут из сети или склада, и пара
запросов к Шикимори на этом фоне ничего не стоит. Повторное открытие
бесплатно: `CHR2_` и `STF3_` бессрочны.

Цена обхода несоразмерна: выписки людей пачкой у Шикимори нет, тридцать
тысяч запросов по одному на тайтл — восемь с половиной часов против
шестичасового потолка задачи. Описания людей в файл всё равно не едут —
авторский текст, — значит рантайм-путь обязателен, и датасет добавил бы
второй путь ради одних имён. Разрез `person-title.ts` по образцу
`media-title.ts` нужен только датасету и снимается из плана.

Этап открывается заново по триггерам, а не по календарю: замеры этапа 2
покажут, что блок людей на чистой установке дорисовывается латиницей
заметно дольше остального, или `shikimori-people.ts` начнёт сбоить
сериями. Тогда — датасет людей скользящими срезами, только имена,
без описаний.

---

## Этап 6. Синхронизация с сервисами

Последний по очереди и пока концептуальный. Не отвергнут — не доказан.

Что мешает взяться раньше: у сервисов разные статусы и шкалы оценок,
нет честного времени правки и признака удаления. И главное — в ядре
нет понятия «подключённый сервис»: `collection.ts` знает только номер
хозяина AniList.

Этап 3 снимает срочность: когда данные уже не в одной копии,
синхронизация становится удобством, а не спасением.

---

## Замеры, которых не хватает

Сняты на этапе 2, числа настоящие:

- **Настоящий размер датасета после gzip** — 1.04 МБ имена
  и 106 КБ карта номеров. В приватном каталоге распакованный файл
  занимает около четырёх мегабайт, под него поднят потолок записи
  файлов в оболочке.
- **Доля тайтлов с русским названием** — 86% от тех, что знает Шикимори,
  а Шикимори знает 98.9% номеров манами.

Сняты шагом 4.0, тоже настоящие:

- **Заголовки и CORS раздачи** — потоку не нужен ни `Referer`, ни `Origin`,
  а `access-control-allow-origin` равен звёздочке и на манифесте,
  и на сегменте. Значит прокси через оболочку в плеере не нужен.
- **Форма потока** — одиночный `VOD` без вариантов внутри, сегменты
  по шесть секунд, около 340 КБ на сегмент в 480p, порядка 240 сегментов
  на эпизод.
- **Сколько живёт ссылка** — не менее двух часов сорока семи минут
  при метке `18`, тридцать замеров подряд с кодом 200. Метка в адресе
  равна часу выдачи плюс два по Москве: выданная в 16:55 несла `18`,
  выданная в 19:37 — `21`.

Остальных у нас нет. Пока их нет, решения принимаются на ощущениях:

- Сколько секунд проходит от открытия списка до кириллицы на всех
  видимых карточках при чистом кэше и без файла датасета.
- Сколько сущностей у типичного человека в списках.
- Какую долю живой коллекции датасет закроет на самом деле. Считается
  не по числу записей в файле, а по тому, сколько строк списка получат
  имя без сети: карта MAL — AniList неполная.
- Как часто меняется адрес приёмника и сдвиг у Kodik. Величины теперь
  добываются на ходу, так что от частоты зависит не работоспособность,
  а только цена промаха.
- **В какой зоне стоит час в подписи ссылки.** Половина ответа есть:
  метка — час выдачи плюс два по Москве, значит упреждающее обновление
  возможно, читая срок прямо из адреса. Открытым остался край: умирает
  ссылка с меткой `18` в 20:00 МСК (зона UTC+1) или в 21:00 МСК (зона UTC).
  До ответа в коде стоит ранний край.

Первые два снимаются за вечер на своём аккаунте и стоят ближе всего
к главному вопросу: насколько вообще велика боль.

Замеры придётся снимать глазами и секундомером: просмотрщика журнала
в приложении нет, а строки датасета уходят типом `DB`, который
в консоль не попадает вовсе.
