# Карта проекта, часть 6: экраны приложения

Продолжение `PROJECT-MAP-DATA.md`. Здесь всё, что лежит в `src/app`:
каркас, тема, экраны, общие собранные куски и слова интерфейса.
Слой данных — в пятой части, оболочка Tauri — в третьей.

Общее правило слоя: экран ничего не знает про сеть и хранение.
Он читает память через `collection-view.ts`, меняет список только
через `queueEdit()`, имена берёт у `media-title.ts`, облик — у `media-looks.ts`,
а взрослое отбирает через `adult.ts`.

---

## Каркас

### main.ts и App.vue

`main.ts` — единственный вход окна `main`, и порядок пуска держит именно он:
сначала подъём настроек, потом монтирование окна, потом подъём списка
из снимка с накатом очереди, и только затем отправщик правок. Настройки
первым шагом не случайно: иначе первая перерисовка идёт на значения
по умолчанию, и тумблеры показа не работают всю жизнь окна. Отправщик
ставится после подъёма списка и тоже не случайно: без его запуска
фоновый разбор очереди и повтор при возврате сети мертвы. Отказ любого
из шагов не роняет окно: он идёт в журнал.

Обновления со сервера на старте нет. Перенос списка идёт только по прямому
действию человека из настроек и замещает местный список целиком:
подключённый сервис — источник истины, и его состояние затирает местное.
Тихой сверки для такого шага нет нарочно: подробности курса —
в `docs/DECISIONS-LISTS.md`.

`App.vue` держит адрес и вход: пропуск берётся окном `login`, выход
чистит память списка.

### AppShell.vue

Шапка липкая, слева логотип разметкой, без картинки и шрифта. Меню — 236
пикселей, на узком окне (≤ 1080) сжимается до 72 и показывает одни значки.
Содержание держит ширину `--am-page-max`: сетка постеров на широком
мониторе иначе растягивается в ленту.

Две кнопки шапки: «Назад» и обновление окна. «Назад» жива на карточке
и на странице студии — остальные экраны наверху дерева. Обновление —
честная перезагрузка окна: в оболочке нет ни адресной строки, ни клавиши F5.

### router

Адресов пять: главная, списки, поиск, настройки, плюс карточка
и страница студии, обе с номером в адресе. Манга своего адреса
не имеет: это подвкладка списков.

Грабли: вид списка в адрес не пишется, поэтому кнопка «К манге»
на главной открывает аниме, а возврат с карточки теряет выбор.

---

## Тема и общие классы

`styles/theme.css` — единственное место цвета, радиуса и тени. Своих цветов
в экранах быть не должно: до единой темы каждый экран красился сам
и совпадал с соседом приблизительно.

Семья классов `am-`: `.am-page` и `.am-bar` — каркас экрана; `.am-panel`
и `.am-btn` с видами `--ghost`, `--soft`, `--wide`; `.am-input` и `.am-search`;
`.am-chip` и `.am-seg` — закладки и переключатели с пометкой `--on`;
`.am-switch` — тумблер настроек; `.am-grid` и `.am-rail` — сетка и полка
постеров; `.am-line` — полоса прогресса; `.am-skeleton` — заглушка
загрузки; `.am-empty` и `.am-error` — пустота и отказ.

Размер постера задан одним токеном `--am-tile`: полка главной и сетка
списков обязаны держать один размер, иначе переход между экранами
выглядит рывком. Обводка фокуса задана общим правилом на `:focus-visible`
и нигде не гасится: без неё переезд на пульт невозможен.

### labels.ts

Все слова интерфейса о закладках и частях — в одном файле. Слова зависят
от типа: аниме смотрят эпизодами, мангу читают главами, и один набор
слов на два типа читался калькой. Ключи закладок — серверные, перевод
только на показ: совпадение с ответом сервиса важнее красоты кода.

---

## Общие собранные куски

### MediaTile.vue

Плитка тайтла одна на главную, списки, поиск и страницу студии. Своего
состояния и запросов у неё нет: всё приходит свойствами, наружу идёт
одно событие открытия. Цвет подложки — цвет обложки из AniList: пока
картинка идёт, место уже окрашено. Метка взрослого рисуется тут же
углом плитки.

### EntrySheet.vue

Окно правки записи: закладка, оценка шагом половины, прогресс, тома,
пересмотры, две даты и личный комментарий. Своей памяти о записи
окно не держит — только черновик комментария; остальное идёт свойствами,
а выбор улетает событием наверх. Две правды об одной записи расходятся
быстро, особенно когда перенос списка приходит при открытом окне.

Комментарий — единственное поле с черновиком и единственное, что уходит
не сразу: отправка на уход из поля и на закрытие окна, чтобы очередь
не набивалась правкой на каждый символ. Закрытие любым способом — кнопкой,
фоном, Escape, размонтированием — прогоняет черновик.

Форма: два столбца, на узком окне (≤ 760) один; высота до 90 процентов
экрана с прокруткой внутри. Цели нажатия — от 44 пикселей: это задел
под пульт, а не вкус. Оценка ставится и шагами, и быстрыми отметками
от 1 до 10.

Даты: ввод браузерный, значение — строка `ГГГГ-ММ-ДД`, рядом кнопка
«сегодня». Пустое значение — это «стереть», и так оно и уезжает в правку.
Отметка «просмотрено» одним действием ставит закладку и доводит счёт
до полного.

### PeopleBox.vue и PersonSheet.vue

Полка персонажей и сетка авторов на карточке; окошко человека поверх
экрана с переходом к сэйю. Русские имена и описания идут фоном через
Шикимори отдельным проходом.

---

## Экраны

### HomeScreen.vue — главная

Две полки из своей памяти: «Продолжаю смотреть» и «Читаю сейчас»,
в каждую идут закладки «смотрю» и «пересматриваю». Сетевых запросов
ради состава полок нет вовсе — только добор облика и имён для показанного.
Главная открывается без сети: список уже поднят из снимка.

Счётчики считаются отборами слоя данных, а не своим перебором: числа
на главной и в закладках списков обязаны совпадать.

Грабли: полки не зовут отбор взрослого, поэтому при выключенном показе
взрослое всё равно видно на главной и в похожем (пункт 3.17 плана).

### ListsScreen.vue — списки

Самый сложный экран. Три ряда управления: тип (аниме или манга),
закладка со счётчиком и порядок показа — обновление, своя оценка,
общая оценка, имя в две стороны, части. Манга и аниме разделены
подвкладкой, а не двумя экранами: источник обоих — одна память.

Строки строятся в три шага и показываются после первого: отбор и порядок
по памяти, затем облик пачкой, затем русские имена по десять. Картинка,
ждущая всех имён, выглядит зависанием на большом списке. Имя без
русского перевода показывается латиницей из снимка, номер — последний
выход.

Длинный список показывается сотнями: наблюдатель ловит хвост за 600
пикселей до конца и добавляет шаг, рядом стоит кнопка с остатком —
без неё добор недоступен с пульта. Подвал всегда говорит три числа:
всего в списке, отобрано, показано.

Грабли: шаг добора сбрасывается при возврате из карточки, а позиция
прокрутки не запоминается.

Поиск в строке ищет только по своему списку, без сети, с паузой четверть
секунды после набора. Запрос в чужой каталог — дело экрана поиска.

### SearchScreen.vue — каталог

Слово само выбирает источник: латиница идёт в AniList, кириллица —
в Шикимори с переводом находок в тайтлы AniList по номерам MAL.
Переключателя источника на экране нет осознанно. У русского пути нет
второй страницы. Своё в выдаче помечается закладкой из памяти.
Сборка строки плитки общая со страницей студии: `app/tile-row.ts`.

Взрослое просеивается единым отбором слоя данных, а скрытое число
говорится вслух, а не тихо теряется. Остаток долга: при выключенном
показе страница выдачи беднее спрошенного, добора взамен отброшенного нет.

### MediaScreen.vue — карточка

Шапка — баннер с обложкой поверх, дальше две колонки: описание
и боковая полоса в 380 пикселей с колонкой записи; на узком окне
(≤ 1180) колонки складываются. Описание — одна колонка шириной до 78
знаков с сохранёнными переводами строк; газетные колонки резали абзац
по строке.

Служебные номера и источник описания пришиты бледным хвостом в конец
описания, своего виджета справки больше нет.

Под колонками — полоса «Производство»: чипы студий ведут на страницу
работ студии внутри приложения, а не наружу; секции нет, когда список
студий пуст. Ниже — люди тайтла: полка персонажей и сетка авторов.

Панель записи — колонка свойств без коробки: тихая кнопка закладки,
полоса доли просмотренного со строкой счёта, факты строками (оценка,
тома, пересмотры, две даты) и комментарий отдельным блоком. Свои
оценка и прогресс дублируются немыми пилюлями в герое. Меняет запись
только окно правки: два места ввода об одном и том же всегда расходятся.

Карточка видит две правды о записи — свою память и ответ сервера вместе
с карточкой. Показывается своя память: там лежит правка, которая ещё
не уехала. Расхождение называется вслух отдельной подсказкой и живёт
недолго: после ответа сервиса истина его.

Каждая правка идёт одним путём: выбор в окне → `queueEdit()` → память
и очередь → отправщик. Экран никогда не зовёт мутацию сам и не ждёт
сети: отметка видна сразу, доставка — забота слоя данных.

### StudioScreen.vue — студия

Работы студии сеткой постеров внутри приложения, страницами с кнопкой
добора. Плитка и её сборка общие с поиском (`app/tile-row.ts`), свои
метки — из памяти, отбор взрослого — общий, скрытое число говорится
вслух. Русские имена добираются тем же фоновым проходом, но двумя
группами: у студии аниме и манга смешаны. В меню экрана нет: на него
ведут чипы карточки.

### SettingsScreen.vue — настройки

Четыре панели.

- «AniList» — вход и выход, имя вошедшего и кнопка переноса списка
  с сервиса. Перенос — единственное место, откуда он запускается,
  и сказано прямо: приехавшее с сервиса замещает местный список.
- «Свои данные» — сколько записей в памяти и чистка местного списка.
- «Показ» — тумблер «Показывать взрослое (18+)». По умолчанию выключен;
  переключение сразу меняет выдачу поиска и списков, ничего не удаляя.
- «О программе» — сведения о сборке и ссылки.

Настройка пишется в хранилище сразу при переключении, без кнопки
«Сохранить»: скрытая вторая правда о включённости тумблера хуже одного
лишнего письма в хранилище.

---

## Общие правила этого слоя

- Отбор, счёт и порядок — в `collection-view.ts`, а не в разметке.
- Отбор взрослого — только через `adult.ts` и только на слое показа.
- Сеть трогается только ради показанного куска и пачками.
- Список меняется только через `queueEdit()`.
- Перенос списка с сервиса — только по прямому действию из настроек.
- Экран не ждёт сети, чтобы снять признак занятости: добор имён идёт
  своим признаком рядом.
- Цели нажатия не мельче 44 пикселей, у любого добора есть кнопка,
  фокус виден — задел под телевизор.
- Цвета и радиусы — только токенами темы.

---

## Где что искать

| Задача                                | Файл                              |
| ------------------------------------- | --------------------------------- |
| Порядок пуска окна              | `app/main.ts`                     |
| Адрес, вход и выход             | `app/App.vue`                     |
| Шапка, меню, «Назад», обновление | `app/components/AppShell.vue`     |
| Плитка тайтла                        | `app/components/MediaTile.vue`    |
| Окно правки записи                   | `app/components/EntrySheet.vue`   |
| Люди тайтла и окошко человека   | `app/components/PeopleBox.vue`    |
| Полки и счётчики главной           | `app/screens/HomeScreen.vue`      |
| Закладки, порядок, добор списка  | `app/screens/ListsScreen.vue`     |
| Поиск по чужому каталогу            | `app/screens/SearchScreen.vue`    |
| Карточка и панель записи           | `app/screens/MediaScreen.vue`     |
| Работы студии внутри приложения  | `app/screens/StudioScreen.vue`    |
| Вход, перенос списка, тумблер 18+ | `app/screens/SettingsScreen.vue`  |
| Цвета, радиусы, общие классы      | `app/styles/theme.css`            |
| Слова закладок и частей            | `app/labels.ts`                   |
| Сборка строки плитки                 | `app/tile-row.ts`                 |
| Адреса экранов                       | `app/router/routes.ts`            |
