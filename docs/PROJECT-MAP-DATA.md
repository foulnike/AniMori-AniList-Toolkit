# Карта проекта, часть 5: слой данных

Продолжение `PROJECT-MAP-SHELL-2.md`. Здесь темп обращений к AniList, хранение
данных пользователя и восемь файлов слоя данных. Подсистемы моста в общем виде
описаны в части 2, оболочка — в частях 3 и 4.

Слой собран, но потребителей у него пока нет: экраны появятся пунктом 3.3.

---

## Темп обращений

### rate-limit.ts

Ограничитель заводится на источник, а не на домен: лимит считается по IP,
и зеркала делят один бюджет. Экземпляры: `shikiLimiter`, `anime365Limiter`,
`animeThemesLimiter` — 300 мс между запросами и 60 за минуту; `anilistLimiter` —
100 мс, старт 30 за минуту, предохранитель 90, интервал считается от потолка.

`acquireSlot()` — единственный вход. Слоты выдаются по одному через шлюз: без
него два параллельных вызова занимают один слот. Ждать приходится по трём
причинам сразу — не вышел интервал, стоит пауза, окно выбрано до потолка.

Плавающий потолок включён только у AniList: остальные источники заголовков
не присылают. `applyCeiling()` принимает снижение всегда, а рост — только когда
кончилось восстановление; `reduceCeiling()` режет потолок вдвое и закрывает рост
на пять минут. Ниже шести запросов в минуту потолок не опускается: серия 429
иначе остановила бы работу вовсе.

### anilist.ts

Единственная точка GraphQL и держатель общей паузы: только клиент видит все
запросы к AniList сразу.

Заголовки читаются из любого ответа, включая ошибки, и в любом регистре имени:
мост в Rust имена понижает, скриптовый отдаёт как пришло. `x-ratelimit-limit`
учит потолок, нулевой `x-ratelimit-remaining` ставит паузу до `x-ratelimit-reset`,
но не длиннее минуты.

Ответ 429: пауза по `retry-after` либо пять секунд, потолок урезается, повтор
до трёх раз. Отказ сервера — это 403, 408 и всё от 500: пауза 30 секунд
с удвоением до пятнадцати минут, любой удачный ответ обнуляет серию. Именно 403
отдаёт AniList, когда закрывает API целиком.

Паузу дольше десяти секунд вызов не высиживает, а бросает ошибку с остатком:
обещание, висящее пятнадцать минут, выглядит зависанием.

Пропуск живёт в хранилище под ключом `AL_TOKEN` и подставляется мостом.
На самом anilist.co токен сессии из Vuex перекладывается в хранилище: иначе мост
о сессии сайта не узнает.

---

## Хранение

Три места, и различает их цена потери.

| Что                             | Где                          | Если потеряется          |
| ------------------------------- | ---------------------------- | ------------------------ |
| Ответы сети, русские карточки   | IndexedDB (`db.ts`)          | добудется заново         |
| Снимок списка и очередь правок  | хранилище моста              | правки взять негде       |
| Второй экземпляр снимка         | файл приватного каталога     | остаётся главный         |

Базы приложения нет вовсе: список пользователя приходит одним запросом
и укладывается в память. Обмена между устройствами нет: источник правды — AniList.

### db.ts — склад ответов

Три стора: `shikiCache` (ключ-строка), `malCache` и `franchiseCache` (ключ-число).
Миграции идут по номеру версии, шаги идемпотентны. Открытие разрешается всегда
и ровно один раз: соединением, `null` по ошибке или `null` по таймауту в семь
секунд. Работа без кэша медленна, но это работа; висящий старт — мёртвое
приложение.

Соединение снимает само себя при `versionchange` и `close`, иначе в памяти
остался бы битый экземпляр до конца сеанса. Сборщик мусора чистит `shikiCache`
курсором по метке времени.

### snapshot.ts — снимок и очередь

Две записи в хранилище моста: `AM_SNAPSHOT` и `AM_EDIT_QUEUE`.

Снимок пишется и читается целиком одной записью — атомарность вместо
транзакций. Чужая версия и битый разбор дают пустой снимок, отдельные битые
записи отбрасываются поштучно. Рядом с записями лежит номер входа: без него
после смены учётной записи показали бы чужой список.

Хозяин один: `ownSnapshot()` принимает поставщика, второй пишущий отклоняется.
Запись откладывается на две секунды — прокрутка списка меняет память десятками
правок в секунду. Точек немедленного сохранения две: уход в фон
(`visibilitychange`) и `pagehide`. Не `beforeunload`: в WebView и на мобилках он
часто не приходит вовсе.

Очередь правок пишется немедленно и целиком: это единственные данные, которые
негде взять заново. Потолок 500 правок, старые вытесняются. У правки свой
идентификатор, а не серверный: отметить принятой надо именно отправленную.
Снимок и очередь идут одной цепочкой записи — параллельные записи обгоняют друг
друга и теряют хвост.

### Дубль снимка в файл

После записи в хранилище и в том же звене цепочки снимок кладётся вторым
экземпляром в `animori-snapshot.json`. Читается он только при пустом хранилище
и тут же возвращается в него, иначе подъём повторялся бы каждый запуск.
Пустой дубль подъёмом не считается.

Отказ файла идёт в журнал и ничего не роняет: без дубля программа работает,
а сорванная запись снимка — потеря. Файл переписывается на каждое сохранение
снимка, включая отложенные.

---

## Слой данных

Порядок запуска: `initCollection()` поднимает снимок и накатывает очередь,
затем `startEditSender()`, затем `refreshFromServer()`. Экраны меняют список
только через `queueEdit()`, а названия просят через `prefetchRussianTitles()`
на видимый кусок и `peekRussianTitle()` на отрисовку строки.

```
экраны
  ├─ collection-view.ts  отборы и счётчики (только чтение)
  ├─ edit-sender.ts      queueEdit → collection.ts → snapshot.ts
  └─ media-title.ts      русские карточки → db.ts, api/anilist-media.ts
collection.ts  ← api/anilist-list.ts
edit-sender.ts → api/anilist-edit.ts
```

### collection.ts — хозяин списка

Записи держатся словарём по номеру тайтла: карточка и каждая правка ищут запись
по номеру, а обход тысяч записей на приставке виден глазом. Ответ сервера —
основа, неотправленные правки накатываются сверху, иначе оценка откатилась бы
на глазах. Обновление замещает содержимое целиком: удалённое на сайте не должно
остаться в памяти навечно. Смена входа чистит память, второе обновление ждёт
идущее, а не шлёт свой запрос. Правка неизвестного тайтла создаёт запись — так
выглядит добавление, которого сервер ещё не видел.

### collection-view.ts — отборы

Только чтение памяти. Условие отбора одно на перебор, подсчёт и страницу:
расхождение выглядело бы как ошибка чисел у закладок. Перебор ленивый,
выдача отдаёт ссылки на записи, а не копии. Сортировка только по полям снимка —
обновление, оценка, прогресс, номер; названий в памяти нет. Ноль оценкой
не считается: так AniList отдаёт «оценки нет». Запись без статуса попадает
в `UNKNOWN`, а не теряется.

### edit-sender.ts — отправка правок

Единственный путь изменений на сервер. `queueEdit()` меняет память, пишет
очередь и толкает разбор. Разбор идёт строго по порядку и в одном потоке: две
правки одного тайтла местами менять нельзя. Отказ по содержанию выкидывает свою
правку — иначе одна плохая запись заперла бы очередь навсегда; временная помеха
останавливает весь разбор до следующего захода. Попытка засчитывается только
при ответе сервера, потолок — пять попыток. Заходы: раз в минуту и на событие
`online`. Пока стоит общая пауза AniList, разбор не начинается вовсе.

### media-title.ts — русские названия

Путь чтения трёхуровневый: память запуска, склад, сеть. В памяти запоминается
и отсутствие перевода, иначе каждая перерисовка била бы в сеть за тем же
отказом; сетевой сбой не запоминается — вечная латиница до перезапуска была бы
дефектом. Карточки лежат на складе с префиксом `RU1_` и живут по общему сроку
годности. Пачка добывается по очереди, а не веером: сотня одновременных запросов
сразу упирается в темп.

### api/anilist-list.ts

`fetchViewer()` — кто вошёл; `fetchUserList()` — вся коллекция одним запросом.
Шкала оценки задана в самом запросе, а не взята из настроек пользователя:
иначе одна и та же запись приходит то как 85, то как 8.5. Деление на подборки
сайта не сохраняется: дубли схлопываются по номеру тайтла, остаётся более
свежая запись. Запись без номера тайтла отбрасывается: ни показать, ни слить
с правкой её нельзя.

### api/anilist-edit.ts

Мутации `SaveMediaListEntry` и `DeleteMediaListEntry`. Незаданные поля
в мутацию не кладутся вовсе: переданный `null` стёр бы поле, которого
не трогали. Удаление стоит двух запросов — сервер удаляет по номеру записи
списка, а в памяти лежит номер тайтла; пропавшая запись считается удалённой.
Ответ сводится к одному вопросу: повторять или бросить.

### api/anilist-media.ts

Мостик AniList → MAL: русские источники знают только номера MyAnimeList.
Спрашивается пачками по пятьдесят — таков потолок страницы, а поодиночке темп
сгорит. Потерянная пачка обход не обрывает. Тайтлы без номера MAL в ответ
не попадают: русского источника для них нет.

---

## Подсистема файлов моста

Контракт `IFiles`: `available`, `read`, `write` с булевым исходом. Ядру нужен
ответ «есть ли диск» без проверки платформы. Наружу идёт имя, а не путь:
каталог выбирает оболочка, и общий код не может попросить себе чужое место.

В десктопе — команды `animori_file_read` и `animori_file_write`; ни чтение,
ни запись не отклоняются, отказ уходит в консоль. В браузере честная заглушка:
`available: false`, `null` и `false` молча. Скачивание файла заменой не служит:
оно требует жеста пользователя и не читается обратно.

`src-tauri/src/files.rs`: имена только из белого списка (`animori-snapshot.json`
и забронированное `animori-queue.json`), каталог — приватный каталог приложения,
он же создаётся при первом обращении. Запись идёт во временный файл
с переименованием: гибель процесса посередине оставила бы обрезок вместо
снимка. Потолок записи — восемь мегабайт. Отсутствие файла ошибкой не считается
и возвращается как «нет данных».

Очередь правок в файл пока не дублируется: имя забронировано впрок.

### Разрешения

Обе команды выданы только окну `main` в `capabilities/default.json`: чужому
сайту нечего читать и переписывать в нашем снимке. Места регистрации те же три,
что у любой команды, — `invoke_handler` в `lib.rs`, `COMMANDS` в `build.rs`,
разрешение в capability нужного окна; для вызова из разметки добавляются
контракт моста и обе его реализации.

---

## Где что искать

| Задача                                | Файл                             |
| ------------------------------------- | -------------------------------- |
| Темп, потолок, паузы                  | `shared/api/rate-limit.ts`       |
| Разбор ответа и отступ AniList        | `shared/api/anilist.ts`          |
| Склад ответов, миграции, сборщик      | `shared/core/db.ts`              |
| Снимок, очередь правок, дубль в файл  | `shared/core/snapshot.ts`        |
| Список в памяти и обновление           | `shared/core/collection.ts`      |
| Отборы, счётчики, средняя оценка      | `shared/core/collection-view.ts` |
| Отправка правок и повторы             | `shared/core/edit-sender.ts`     |
| Русские названия и описания           | `shared/core/media-title.ts`     |
| Запросы списка, правок, номеров MAL   | `shared/api/anilist-*.ts`        |
| Файлы приложения                      | `src-tauri/src/files.rs`         |
