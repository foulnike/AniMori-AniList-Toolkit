# Архитектура: видео и плеер

Часть документа «Архитектура». Обзор, слои, мост, слой запросов и хранение —
в `ARCHITECTURE.md`; экраны и интерфейс — в `ARCHITECTURE-UI.md`; оболочка Tauri
и сборка — в `ARCHITECTURE-SHELL.md`.

---

## Сейчас просмотра нет

В приложении нет ни своего плеера, ни встроенного окошка партнёра.
Прежняя схема — скин вокруг чужого кадра, где нашими были только сайдбар
озвучек и сетка серий, — жила в юзерскрипте и уехала вместе с ним
в ветку `script`.

Возвращать её в приложение незачем, и причин три:

- **Полный экран принадлежит чужому кадру.** Мы отдаём разрешение
  на полный экран и автовоспроизведение — и на этом наше участие
  кончается.
- **Фокуса внутри кадра у нас нет.** Элементы чужого интерфейса — не кнопки,
  а `div`, и с пульта до них не добраться. Для приставки это приговор.
- **Всё нужное от плеера приходится просить сообщением** и надеяться
  на ответ. Словарь методов нигде публично не описан, полного списка
  у нас нет.

Пустое место здесь выгоднее унаследованного скина: плеер пишется сразу
как экран приложения — со своим полным экраном и своей моделью фокуса.

---

## Куда идём: свой плеер на прямых ссылках

Решение от 20.08.2026: **тянуть прямые ссылки на поток и из Kodik тоже,
а плеер писать свой** — с непосредственным доступом к его внутреннему
интерфейсу. Тогда Kodik и Aniliberty становятся равноправными: оба просто
отдают ссылку на поток, а интерфейс наш целиком.

Aniliberty при этом перестаёт быть «фоллбэком». Это единственный источник,
где прямые ссылки лежат в открытом API, поэтому свой плеер пишется
и отлаживается на нём, а резолвер Kodik подключается к готовому плееру.
Обратный порядок означает отладку своего плеера и чужого формата
одновременно.

Абстракция одна:

```
VideoSource
  ├─ KodikResolver       номер тайтла → озвучки, серии, ссылка на поток
  └─ AniliberyResolver   номер тайтла → озвучки, серии, ссылка на поток
```

Выше резолверов плеер не знает, чей поток играет. Ниже — каждый резолвер
сам разбирается со своим сервисом. Тот же приём, что и мост: разница
уходит в реализацию, а не в вызывающий код.

---

## Цепочка добычи прямой ссылки у Kodik

Пройдена руками на живом тайтле 01.09.2026 — проба 4.0, тайтл `shikimori_id=20`,
озвучка AniDUB, серия 1. Ниже измерения, а не чтение чужого кода: половина
прежнего описания в этом файле оказалась неверной, и правки отмечены прямо
в шагах. Шесть шагов.

1. **Поиск.** `GET kodik-api.com/search` с токеном и `shikimori_id`, плюс
   `with_seasons`, `with_episodes` и **`with_episodes_data`**. Последнее поле
   обязательно: без него значение карты серий — пустая строка или номер,
   а с ним объект со ссылкой на серию и пятью превью на `i.kodikres.com`.
   Ответ 200, пять озвучек, у каждой своя карта серий.
   Клиента Kodik в этой ветке нет вовсе — ни `kodik.ts`, ни `collectEpisodes`,
   ни `Translation.episodeLinks`. Разбор, сохраняющий адреса серий, живёт
   в ветке `script`, и здесь его придётся написать заново.
2. **Три поля из адреса.** Из ссылки `//kodikplayer.com/seria/176463/f91684…/720p`
   регуляркой `/\/(video|seria|serial)\/(\d+)\/([0-9a-z]+)/i` достаются тип, номер
   и хэш. Тип бывает **`seria`**, а не только `video`, как считалось раньше;
   качество стоит в конце пути и на дальнейшее не влияет. Сети на этом шаге
   не нужно вовсе.
3. **Страница серии.** `GET` того же адреса. Здесь лежит то, без чего приёмник
   не отвечает: `domain`, парные подписи `d_sign` и `pd_sign`, `ref` с подписью
   `ref_sign`, а также `translationId`, `translationTitle`, готовые превью
   и — главное — имя связки в `playerLink` вида
   `/assets/js/app.player.<sha256>.js`. Имя связки меняется с каждым их выпуском,
   поэтому берётся регуляркой из тела страницы: транскрибировать его руками
   бесполезно. Запасной путь для трёх полей шага 2 — присваивания `vInfo.*`
   в этой же странице.
4. **Связка: адрес приёмника и шифр.** Оба спрятаны, но не в связке целиком,
   а в вызовах `atob` со строками base64. На момент проверки:
   `L2Z0b3I=` раскрывается в `/ftor`, `Ly8oPzpnZXR8Y2xvdWQpLmtvZGlrLWNkbi5jb20=` —
   в регулярку `//(?:get|cloud).kodik-cdn.com`. **Вторая к раздаче не относится:**
   это проверка внутреннего флажка, домен потока у неё другой. Рядом в открытом
   виде лежит и шифр расшифровки. Правильное поведение — держать найденное
   в памяти и переоткрывать связку при первой же неудаче запроса.
5. **Запрос на приёмник.** `POST` формой на найденный адрес, с `X-Requested-With`
   и заголовками страницы. Поля: `d`, `d_sign`, `pd`, `pd_sign`, `ref`, `ref_sign`,
   `bad_user`, `cdn_is_working`; необязательно `uid`, `type`, `hash`, `id`, `link`.
   Ответ 200: `default` с номером качества по умолчанию (у пробы 360) и `links`,
   разложенные по качествам, каждое со своим зашифрованным `src` и типом
   `application/x-mpegURL`.
6. **Расшифровка.** Сдвиг по латинским буквам с переносом через `z`, затем base64
   с добиванием до кратности четырём, затем при нужде дописывается схема.
   Декодировать нужно только строку **без** `//` — незашифрованный ответ шифр
   сломает. Признак удачи однозначен: в результате есть `.m3u8`. Перебор 1..25
   на трёх качествах дал ровно одно подходящее значение, так что подбор
   безошибочен. **Каждое качество расшифровывается отдельно:** прежнее описание
   предлагало подменить номер качества строкой в готовом адресе — так делать
   нельзя, у качеств разные площадки.

### Что отдаёт CDN

Поток раздаёт **`solodcdn.com`**, а не `kodik-cdn.com`, и номер площадки плавает
даже между качествами одной серии: у пробы 360 приехало с `p12`, 480 и 720 —
с `p14`. Поэтому в разрешениях стоят шаблоны по поддоменам, а не точные имена.

Адрес выглядит так (сокращённо):

```
https://p14.solodcdn.com/s/m/<base64 пути>/<sha256>:2026090118/480.mp4:hls:manifest.m3u8
```

Отрезок после двоеточия — срок годности с точностью до часа, лежит в ссылке
открытым текстом.

Замеры 01.09.2026:

- **Заголовки потоку не нужны.** Манифест отдаётся с кодом 200 без `Referer`,
  без `Origin` и с любым чужим `Origin`, включая `tauri://localhost`.
- **CORS открыт настежь:** `access-control-allow-origin: *` и на манифесте,
  и на сегменте. Значит библиотека `m3u8` тянет поток прямо из WebView,
  и проксировать воспроизведение через Rust не нужно.
- **Манифест одиночный, не master:** `PLAYLIST-TYPE:VOD`, `VERSION:3`,
  `TARGETDURATION:6`. Вариантов внутри нет, автовыбора битрейта не будет —
  переключение качества делаем сами подменой источника с восстановлением позиции.
- **Адреса сегментов относительные** и вида `./480.mp4:hls:seg-1-v1-a1.ts`.
  Точка со слэшем впереди не украшение: без неё двоеточие в имени можно принять
  за схему адреса.
- Манифест приходит как `text/plain`, сегмент — как `application/octet-stream`;
  сегмент на 6 секунд весит около 340 КБ в 480p, эпизод на 24 минуты —
  порядка 240 сегментов. `Range` сервер игнорирует, `cache-control` не присылает.
- У AniDUB **ключи 480 и 720 ведут на один и тот же файл**. Список качеств
  из ответа — не истина, качества надо сворачивать по адресу, иначе в интерфейсе
  появится выбор «720», который на деле 480.

Сам Kodik настраивает библиотеку так: `maxBufferSize: 7e7`, `maxBufferLength: 30`,
`maxMaxBufferLength: 60`, `liveSyncDuration: 30`. Значения разумные, берём за основу.

### Что здесь хрупко

- **Ссылки подписаны и живут недолго.** Кэшировать поток нельзя ни при каких
  условиях. Просроченная ссылка — норма, а не ошибка: правильная реакция —
  молча пройти цепочку заново и продолжить с той же секунды. Ссылка, взятая
  за минуту до часовой границы, умрёт на первой минуте эпизода, поэтому
  обновление на ходу обязательно, а не «на всякий случай».
- **Адрес приёмника, шифр и сдвиг — подвижные величины.** Сдвиг измерен, но
  в код не попадёт даже подсказкой: он добывается перебором, удачное значение
  живёт в хранилище, при промахе перебор идёт заново. Стоимость — двадцать пять
  декодов base64, то есть ничего.
- **Имя связки со `sha256` меняется с каждым их выпуском** — только регуляркой
  из страницы.
- **Номер тайтла приходит не оттуда, откуда кажется.** Поиск идёт
  по `shikimori_id`, а передаётся туда `idMal`. Работает потому, что номера
  у Шикимори и MAL совпадают. При смене источника связь отвалится.
- **Чужой протокол не документирован.** Ломается он молча и в любой момент.
- **Чужой код брать нельзя.** Реализация, по которой цепочка сверялась, выходит
  под лицензией семейства GPL, а у нас MIT. Пишется заново по описанию
  протокола, а не копированием.

### Что нужно поправить в своём коде

- В `capabilities/default.json` открыты `kodik-api.com` и `kodikplayer.com` —
  поиск, страница серии и приёмник, — а также `*.solodcdn.com`,
  `*.kodik-cdn.com` и `i.kodikres.com` для раздачи и превью. Сделано
  01.09.2026 по итогам пробы.
- Через мост идут только три первых шага: поиск, страница серии и `POST`
  на приёмник. Им нужен контроль заголовков и обход правил источника, поэтому
  запрос из Rust по образцу `animori_anilist_query`. Сам поток библиотека
  забирает напрямую — CORS это позволяет.
- Плеер — новый экран, а не виджет в разметке: имя в `SCREEN_NAMES`,
  подпись в `SCREEN_TITLES`, компонент в `App.vue` (`ARCHITECTURE-UI.md`). В меню
  он не нужен: попадать туда со страницы тайтла.
- Появляется первая жёсткая внешняя зависимость — библиотека для `m3u8`:
  штатно этот формат играет только Safari, WebView2 сам не умеет. Вес её
  здесь почти не важен: она едет внутри установщика, а не по сети
  каждому открытию страницы.

### Чего это стоит

Прямая ссылка убирает рекламу партнёра целиком и без тумблера. Записано
здесь для того, чтобы это осталось решением, а не побочным эффектом,
обнаруженным после выпуска.

---

## Блокировщика рекламы здесь нет

Три механизма версий 2.x — скрытие баннеров стилями, отсечение запросов
в `adblock.rs` и отсечение всплывающих окон — сняты при разделении
продуктов целиком. Работающими они остались только в замороженном `main`
и в выпущенных сборках 2.x.

Два следа этого снятия важны для плеера:

- Разрешённые адреса цепочки доставки жили в списке блокировщика
  и проверялись раньше примет. Теперь такое место одно — разрешения http
  в `capabilities/default.json`.
- Единственная чужая страница в приложении — окно входа. Если реклама
  там когда-нибудь помешает, решение принимается заново: прежний список
  для этого не годился — большая его часть описывала биржи самого
  anilist.co, которых приложение не открывает вообще.
