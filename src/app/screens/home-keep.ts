// Отбор главной живёт вне показа экрана: возврат с карточки
// собирает экран заново и сбрасывал выбранный жанр витрины.
import { ref } from 'vue'

/** Выбранный жанр витрины. Пустая строка — весь каталог. */
export const homeGenre = ref('')
