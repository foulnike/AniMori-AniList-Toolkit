// Десктопная цель псевдопути '@adblock-impl'.
//
// Тот же приём, что и с '@bridge-impl': реализацию выбирает resolve.alias по mode
// сборки, а не проверка платформы внутри модуля. Ветвление в рантайме здесь
// не годится: initAdblock() тянет за собой наблюдатель мутаций, строку стилей
// и net-block.ts, и Rollup вправе сохранить эти побочные эффекты даже после удаления
// недостижимой ветки.
//
// В юзерскриптной сборке вместо этого файла подставляется impl.noop.ts, и весь
// блокировщик вместе с разведчиком не попадает в граф модулей: рекламу в браузере
// режет расширение пользователя, а сетевой половины там нет и быть не может.

export { destroyAdblock, getBlockedCount, initAdblock, syncAdblock } from './index'
export { destroyNetProbe, initNetProbe } from './net-probe'
