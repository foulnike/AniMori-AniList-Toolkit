// Одноразовый патч документации, часть 1. Итог сплошной сверки документов
// с кодом: правки, которые не проходят записью через API.
//
// Запускать из корня ветки app-3.0-dev:
//   node scripts/docs-fix.mjs
//
// Каждый якорь ищется дословно. Не нашёлся или нашёлся дважды — файл
// не пишется вовсе, а имя правки печатается: значит документ уже правили
// руками и правку надо переносить глазами.

import { existsSync, readFileSync, writeFileSync } from 'node:fs'

/** Многострочный якорь: строки задаются по одной, чтобы не экранировать переводы. */
const P = (...lines) => lines.join('\n')

const swap = (name, find, to) => ({ kind: 'swap', name, find, to })
const line = (name, starts, to) => ({ kind: 'line', name, starts, to })
const after = (name, starts, add) => ({ kind: 'after', name, starts, add })
const para = (name, has, to) => ({ kind: 'para', name, has, to })

const files = [
  {
    file: 'docs/README.md',
    edits: [
      swap(
        'остатки манги в ядре',
        P(
          'Незакрытым остался код, а не тексты. В ядре ещё живут правила перевода',
          'глав и томов и настройки манговой читалки — обращаться им некуда: ни',
          'переводчика, ни манги в этой ветке нет. Где что чистить — в `ROADMAP.md`.',
        ),
        P(
          'Незакрытым остался код, а не тексты. В ядре ещё живут правила перевода',
          'глав и томов — `rxAct`, `rxLabel` и `rxUnit` в `core/constants.ts`, — плюс',
          'ключи внешней ссылки на чужую читалку: `set_link_mangalib`',
          'и `set_mangalib_domain` в `core/settings.ts`. Своей читалки не было',
          'никогда, и её настроек в ядре нет. Обращаться этому коду некуда: ни',
          'переводчика, ни манги в этой ветке нет. Где что чистить — в `ROADMAP.md`.',
        ),
      ),
      swap(
        'размер документов',
        P(
          'Часть документов крупнее 25 КБ и правится локально, а не через API —',
          'почему именно так, сказано в `CONVENTIONS.md`, раздел «Размер файла».',
        ),
        P(
          'Часть документов правится локально, а не через API: запись целиком',
          'у них уже не проходит, а рвётся молча и посередине. Порога в байтах',
          'при этом нет — почему, сказано в `CONVENTIONS.md`, раздел «Размер файла».',
        ),
      ),
    ],
  },
  {
    file: 'docs/REPO-LAYOUT.md',
    edits: [
      line(
        'тег приложения',
        '| `app-3.0-dev` |',
        '| `app-3.0-dev` | настольное приложение со своим интерфейсом (Vue + Tauri) | будет `app-3.0.0`, выпуска ещё нет |',
      ),
      line(
        'тег скрипта',
        '| `script` |',
        '| `script` | пользовательский скрипт для Tampermonkey | будет `script-2.1.0`, выпуска ещё нет |',
      ),
      line(
        'теги монолита',
        '| `main` |',
        '| `main` | история монолита 2.x и словарь перевода | `v2.0.2` и раньше — единственные, что есть |',
      ),
      swap(
        'что делает build:app',
        P(
          'теперь конфиг один и безусловный. `npm run build:app` собирает только',
          'передний край в `dist/app` — его же зовёт Tauri через `beforeBuildCommand`.',
        ),
        P(
          'теперь конфиг один и безусловный. `npm run build:app` сначала гоняет',
          '`typecheck:all`, потом собирает передний край в `dist/app`. Его зовёт',
          'и `beforeBuildCommand`, и `beforeDevCommand`, поэтому `tauri:dev` каждый',
          'раз пересобирает разметку целиком: сервера разработки с горячей',
          'подстановкой в окне нет, окно всегда берёт готовые файлы.',
        ),
      ),
    ],
  },
  {
    file: 'docs/ARCHITECTURE-SHELL.md',
    edits: [
      swap(
        'внешние ссылки',
        P(
          'Внешние ссылки перехватываются в `on_navigation` и уходят в системный',
          'браузер: иначе окно уехало бы на сторонний сайт без адресной строки,',
          'и человек остался бы в браузере без кнопки «назад».',
        ),
        P(
          'Внешние ссылки уходят в системный браузер командой',
          '`animori_open_external`: разметка зовёт её сама, команда проверяет схему',
          'и передаёт адрес плагину `opener`. Перехвата навигации (`on_navigation`)',
          'в коде нет вовсе — окно грузит только свою сборку, и уезжать ему некуда.',
          'Причина у самой команды другая: в WebView2 `target="_blank"`',
          'и `window.open()` отбрасываются МОЛЧА — ни окна, ни ошибки, ни события.',
        ),
      ),
      swap(
        'исходы прокси',
        P(
          '`proxy.rs` сводит всё к одному из четырёх исходов: выключено, неверные',
          'данные, адрес не отвечает, применено. Исход отдаётся интерфейсу командой:',
          'молчаливое применение нерабочего прокси выглядит как поломка приложения.',
        ),
        P(
          '`proxy.rs` сводит всё к одному из четырёх исходов: выключено, неверные',
          'данные, адрес не отвечает, применено. Исход отдаётся интерфейсу командой:',
          'молчаливое применение нерабочего прокси выглядит как поломка приложения.',
          '«Применено» значит ровно одно: движок получил адрес. Прокси, который',
          'принимает соединение и не пускает наружу, TCP-щупом не отличить.',
          'За пределами Windows этого исхода не бывает вовсе — переменную окружения',
          'читает только WebView2. Состояние авторизации отдаётся отдельно и живым',
          'опросом: не спрашивали, спросили, приняли, отвергли.',
        ),
      ),
      swap(
        'порядок шагов сборки',
        P(
          'Порядок шагов держит сам Tauri: `beforeBuildCommand` зовёт',
          '`npm run build:app`, а `frontendDist` смотрит на `dist/app`. Соблюдать',
        ),
        P(
          'Порядок шагов держит сам Tauri: `beforeBuildCommand` и `beforeDevCommand`',
          'зовут `npm run build:app`, а `frontendDist` смотрит на `dist/app`. Сам',
          '`build:app` начинается с `typecheck:all`, поэтому красный тайпчек роняет',
          'и сборку, и запуск окна. Сервера разработки в конфиге нет вовсе. Соблюдать',
        ),
      ),
      swap(
        'состав профиля',
        P(
          '`: файл настроек,',
          'пропуск, копия снимка. Имя каталога — это `identifier` из конфига,',
        ),
        P(
          '`. В нём файл настроек `animori-settings.json` — там же лежит',
          'и пропуск AniList, ключи `auth_token` и `auth_expires_at`. Рядом дубль',
          'снимка `animori-snapshot.json` и датасет названий `animori-dataset.json`',
          'на четыре с лишним мегабайта.',
          '',
          'Имя каталога — это `identifier` из конфига,',
        ),
      ),
    ],
  },
  {
    file: 'docs/ARCHITECTURE-VIDEO.md',
    edits: [
      swap(
        'клиент Kodik в этой ветке',
        P(
          '   серии.** Раньше `collectEpisodes` выбрасывал эти значения и оставлял одни',
          '   номера. Сейчас адреса лежат в `Translation.episodeLinks`, а `episodeLink()`',
          '   достаёт адрес по номеру серии. Вход в цепочку открыт, и лишнего',
          '   запроса резолверу не понадобится.',
        ),
        P(
          '   серии.** Это описание чужого протокола, а не нашего кода: клиента Kodik',
          '   в этой ветке нет вовсе — ни `kodik.ts`, ни `collectEpisodes`,',
          '   ни `Translation.episodeLinks`. Разбор, который сохраняет адреса серий,',
          '   живёт в ветке `script`, и здесь его придётся написать заново. Зато вход',
          '   в цепочку открыт с первого шага: лишнего запроса резолверу не нужно.',
        ),
      ),
      swap(
        'разрешённые адреса Kodik',
        P(
          '- `capabilities/default.json` не знает домена раздачи потока. Прежде он был',
          '  ещё и в разрешённых адресах блокировщика; блокировщик снят, и место',
          '  осталось одно. Без записи в разрешениях http запрос из Rust не пройдёт.',
        ),
        P(
          '- В `capabilities/default.json` уже открыты `kodik-api.com`',
          '  и `kodikplayer.com` — поиск и страница плеера. Домена раздачи самого',
          '  потока там нет, а он и есть последнее звено цепочки. Прежде такие адреса',
          '  жили ещё и в списке блокировщика; блокировщик снят, и место осталось',
          '  одно. Без записи в разрешениях http запрос из Rust не пройдёт.',
        ),
      ),
    ],
  },
  {
    file: 'docs/ARCHITECTURE.md',
    edits: [
      swap(
        'источники в шапке',
        P(
          'AniMori склеивает несколько источников в один интерфейс: каталог и списки',
          'берутся у AniList, русские названия и описания — у anime365 и Шикимори,',
          'персонажи и авторы — у Шикимори, видео — у Kodik и в планах у Aniliberty,',
          'музыка — у AnimeThemes.',
        ),
        P(
          'AniMori склеивает несколько источников в один интерфейс: каталог и списки',
          'берутся у AniList, русские названия — из собранного заранее датасета,',
          'а чего в нём нет — у anime365 и Шикимори; описания у них же, персонажи',
          'и авторы — у Шикимори, музыка — у AnimeThemes. Видео нет: плеера',
          'в приложении не существует, и писаться он будет на Kodik с Aniliberty —',
          '`ARCHITECTURE-VIDEO.md`.',
        ),
      ),
      swap(
        'белый список файлов',
        P(
          'и общий код не может попросить себе чужое место. Имена берутся из белого',
          'списка — `animori-snapshot.json` и забронированное впрок',
          '`animori-queue.json`. Запись идёт во временный файл с переименованием:',
        ),
        P(
          'и общий код не может попросить себе чужое место. Имена берутся из белого',
          'списка в `files.rs`, и их три: `animori-snapshot.json` (дубль снимка),',
          '`animori-dataset.json` (датасет названий) и забронированное впрок',
          '`animori-queue.json`. Потолок записи — 8 МБ, поднят под датасет.',
          'Запись идёт во временный файл с переименованием:',
        ),
      ),
      after(
        'ограничитель GitHub',
        '| Шикимори, anime365, AnimeThemes',
        '| GitHub: опись и файлы датасета           | 300 мс   | 60                          |',
      ),
      swap(
        'счёт мест хранения',
        'Три места, и различает их цена потери.',
        'Четыре места, и различает их цена потери.',
      ),
      after(
        'датасет в таблице хранения',
        '| Второй экземпляр снимка',
        '| Датасет русских названий       | файл приватного каталога | скачается заново   |',
      ),
      swap(
        'версия снимка и malId',
        'Форма снимка — шестая версия: из записей ушли вид тайтла и тома.',
        P(
          'Форма снимка — шестая версия: из записей ушли вид тайтла и тома, а номер',
          'MAL (`malId`) добавлен позже без поднятия версии — поле необязательное,',
          'и старый снимок от его отсутствия не ломается.',
        ),
      ),
      swap(
        'порядок пуска',
        P(
          'и накат очереди, — затем `startEditSender()`.',
        ),
        P(
          'и накат очереди, — затем `startEditSender()`. Рядом, не задерживая пуск,',
          'поднимается датасет названий: `initDatasetNames()` читает файл с диска,',
          'а `updateDatasetNamesInBackground()` сверяет опись выпуска и тянет файлы',
          'фоном, если она новее. Ни то, ни другое пуск не блокирует: датасет —',
          'ускорение, а не условие работы.',
        ),
      ),
      swap(
        'путь чтения имени',
        'Путь чтения трёхуровневый: память запуска, склад, сеть.',
        P(
          'Путь чтения имени четырёхуровневый: память запуска, датасет названий,',
          'склад, сеть. Датасет спрашивается через `lookupDatasetName`, и ответ',
          '«такого номера в файле нет» тоже запоминается: в сеть за ним не идут',
          'вовсе — пустое поле в датасете это ответ, а не пропуск. Путь описания',
          'и оценок остался трёхуровневым: память, склад карточек, сеть.',
        ),
      ),
      line(
        'правила глав и томов',
        '- Правила переводчика для глав и томов',
        '- Правила переводчика для глав и томов: `rxAct`, `rxLabel` и `rxUnit`',
      ),
      swap(
        'строки про видео в таблице',
        P(
          'Строка про Kodik изменилась осознанно: пока плеер был скином, заменить его',
          'было нечем. Со своим плеером и двумя резолверами незаменимым остаётся',
          'только AniList — всё остальное имеет второй источник или необязательно.',
        ),
        P(
          'Две строки про видео — Kodik и раздача потока — записаны на будущее:',
          'кода, который к ним ходит, в этой ветке нет, хотя в разрешениях http уже',
          'открыты `kodik-api.com` и `kodikplayer.com`. Записаны осознанно: пока',
          'плеер был скином, заменить его было нечем, а со своим плеером и двумя',
          'резолверами незаменимым остаётся только AniList — всё остальное имеет',
          'второй источник или необязательно.',
        ),
      ),
      swap(
        'датасет идёт путём описи',
        P(
          'ли установщик. Датасет названий пойдёт тем же путём, но там запасной',
          'адрес обязателен.',
        ),
        P(
          'ли установщик. Датасет названий этим путём уже идёт: `api/dataset.ts`',
          'читает опись выпуска, сверяет sha256 и заменяет файл целиком. Запасного',
          'адреса у датасета нет до сих пор — это открытый долг, а не решение.',
        ),
      ),
    ],
  },
  {
    file: 'docs/ROADMAP.md',
    edits: [
      para(
        'граница записи через API',
        '24 КБ',
        P(
          'Правки крупных файлов через API рвутся: запись отвечает успехом, а текст',
          'молча теряет середину. Порога в байтах при этом нет — запись документа',
          'на 26 143 байта прошла целиком и проверена перечитыванием, а обрыв',
          'случался и на меньших файлах. Единственная защита — перечитать',
          'записанное; если файл уже не пишется одной операцией, правка делается',
          'локально и приезжает обычным коммитом.',
        ),
      ),
    ],
  },
]

function applyEdit(text, edit) {
  if (edit.kind === 'swap') {
    const at = text.indexOf(edit.find)
    if (at < 0) return { error: 'якорь не найден' }
    if (text.indexOf(edit.find, at + 1) >= 0) return { error: 'якорь встречается дважды' }
    return { text: text.slice(0, at) + edit.to + text.slice(at + edit.find.length) }
  }

  if (edit.kind === 'line' || edit.kind === 'after') {
    const rows = text.split('\n')
    const hits = []
    rows.forEach((row, i) => {
      if (row.startsWith(edit.starts)) hits.push(i)
    })
    if (hits.length !== 1) return { error: `строк с таким началом: ${hits.length}` }
    if (edit.kind === 'line') rows[hits[0]] = edit.to
    else rows.splice(hits[0] + 1, 0, edit.add)
    return { text: rows.join('\n') }
  }

  if (edit.kind === 'para') {
    const blocks = text.split('\n\n')
    const hits = []
    blocks.forEach((block, i) => {
      if (block.includes(edit.has)) hits.push(i)
    })
    if (hits.length !== 1) return { error: `абзацев с этим текстом: ${hits.length}` }
    blocks[hits[0]] = edit.to
    return { text: blocks.join('\n\n') }
  }

  return { error: 'неизвестный вид правки' }
}

if (!existsSync('docs/CONVENTIONS.md')) {
  console.error('Запускать из корня репозитория: docs/CONVENTIONS.md не найден.')
  process.exit(1)
}

let bad = 0

for (const item of files) {
  if (!existsSync(item.file)) {
    console.error(`НЕТ ФАЙЛА ${item.file}`)
    bad++
    continue
  }

  let text = readFileSync(item.file, 'utf8')
  const errors = []

  for (const edit of item.edits) {
    const result = applyEdit(text, edit)
    if (result.error) errors.push(`    ${edit.name}: ${result.error}`)
    else text = result.text
  }

  if (errors.length > 0) {
    console.error(`НЕ ТРОНУТ ${item.file}`)
    errors.forEach((e) => console.error(e))
    bad += errors.length
    continue
  }

  writeFileSync(item.file, text, 'utf8')
  console.log(`готово ${item.file}: правок ${item.edits.length}`)
}

console.log(
  bad > 0
    ? `\nНеприменённых правок: ${bad}. Файлы с ошибками не изменены, остальные записаны.`
    : '\nВсе правки применены. Проверь git diff и коммить.',
)

process.exit(bad > 0 ? 1 : 0)
